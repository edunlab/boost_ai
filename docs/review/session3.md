# Session 3 Presentation Slides Content
This file is auto-generated for content verification purposes.
---
## Slide 1: [세션 오프닝] 3차시. 데이터 파이프라인 & 벡터 DB데이터 파이프라인 & 벡터 DB [Divider Slide]
- **Category**: 1일차 | 3차시. 데이터 파이프라인 & 벡터 DB

### Content
DAY 1SESSION 03
## 
외부 지식 데이터베이스와 결합하여 고품질의 정보를 탐색하고 처리하는 아키텍처
- **Topic**: 01데이터 전처리 실무
- **Topic**: 02Vector DB & 임베딩 이해
- **Topic**: 03RAG 아키텍처 구축
3차시. 데이터 파이프라인 & 벡터 DB 세션 시작
외부 지식 데이터베이스와 결합하여 고품질의 정보를 탐색하고 처리하는 아키텍처

**Presenter Notes**:
> 3차시에서는 에이전트가 외부 지식 데이터를 불러와 똑똑하게 동작할 수 있게 해주는 RAG 시스템의 핵심인             데이터 파이프라인과 벡터 DB를 알아봅니다.

**Visual & Layout Guide**:
> 동적인 네온 블루/핑크 그라데이션 배경. 데이터 파이프라인 및 노드 흐름을 모티브로 한 그래픽             레이아웃.

---

## Slide 2: 3차시 도입 & 아젠다
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
- **3차시 주제:**출력 구조 제어 & 데이터화
- **오늘 배울 내용:**
  - 비정형 줄글 응답의 비즈니스적 한계 극복
  - 3대 대표 출력 포맷의 특징과 용도 (Table, Bullet, JSON)
  - LLM 응답을 완벽하게 제어하여 데이터 파이프라인과 연동하는 법

**Presenter Notes**:
> 3차시를 시작하겠습니다. 아무리 훌륭한 문장을 작성하는 에이전트가 있더라도, 그 결과가 1,000자짜리               줄글 형태로 나온다면 실무자는 이를 다시 복사해서 엑셀에 넣거나 시스템에 일일이 쳐 넣어야 할 것입니다. 이번 시간에는 LLM의 답변을 우리가 원하는 정확한 규격으로 인출하는 '출력 제어               기술'을 다뤄봅니다.

**Visual & Layout Guide**:
> 정돈된 데이터 그리드와 코드 브래킷(`{ }`)을 은은한 블루 그라데이션 배경에 얹은 테크니컬한               테마 디자인.

---

## Slide 3: 도입 질문: 줄글 응답의 한계
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
- **도입 질문:**"아무리 훌륭한 보고서 요약본이라도, 복사해서 공유하거나 ERP 시스템에 바로 등록하기 어려운 근본적인 이유는                 무엇일까요?"
- **문제점 진단:**
  - **가독성 저하:**긴 텍스트 덩어리는 핵심 파악에 시간이 걸림.
  - **재사용 불가:**텍스트 사이에 숨어 있는 일정, 담당자 정보를 컴퓨터가 자동으로 추출해 내기 어려움.
  - **비정형성의 한계:**실행할 때마다 포맷이 달라져 후속 프로세스 자동화가 불가능함.

**Presenter Notes**:
> 인간에게 유용한 지식은 정제되고 규격화된 상태로 존재합니다. 만약 AI가 쓴 보고서 요약본이 매번 서술               방식이 달라진다면, 그것은 자동화가 아니라 또 다른 수작업의 시작입니다. 비즈니스 에이전트는 출력의 포맷을 강하게 고정해야만 합니다.

**Visual & Layout Guide**:
> 정리되지 않은 뒤엉킨 텍스트 더미가 필터를 통과하면서 깔끔한 스프레드시트로 정돈되는 모션               다이어그램.

---

## Slide 4: 핵심 설명: 3대 출력 포맷 (Table, Bullet, JSON)
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
Table (정형 비교)Markdown다중 속성이나 비교 분석 데이터를 한눈에 정돈해 볼 수 있도록 구성할 때 가장 유용합니다.Bullet List (핵심 요약)List핵심 위주 요약이나 절차 단계 정의, 강의 교안의 주요 메시지 요약 등에 적합합니다.JSON (시스템 연동)API / DB백엔드 데이터베이스, 혹은 다음 API 워크플로우에 인풋으로 직렬화할 때 필수적입니다.

**Visual & Layout Guide**:
> 세 개의 열을 나누어 각각 표 화면, 불릿 텍스트, JSON 코드 블록 예시를 깔끔하게 배치하여               가독성을 높임.

---

## Slide 5: 핵심 설명: 출력 구조 제어 프롬프트 기법
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
Markdown 구조화 프롬프트 템플릿복사
```python
# Role
You are a senior system analyst.
# Instructions
1. Analyze the system logs below.
2. Identify warning and error events.
# Rules
- Do NOT output debug logs.
- Format results in a markdown table.
```

**Visual & Layout Guide**:
> '가드레일 지시어'와 'Few-shot 템플릿' 예제가 하이라이트 표시된 프롬프트 코드 구조도.

---

## Slide 6: 깊이 보기: Few-shot 기법을 통한 출력 보장
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
Few-Shot 프롬프트 구조화 예시복사
```python
사용자 문장을 공손한 존댓말로 변환하는 작업입니다.
Input: "이거 환불해줘."
Output: "죄송하지만, 해당 상품에 대해 환불 처리가 가능할지 확인을 부탁드립니다."
Input: "언제 배송되냐?"
Output: "주문하신 상품의 배송 예정일이 언제쯤인지 안내받을 수 있을까요?"
Input: "물건 이상해."
Output:
```
```python
`## 입출력 예시
[입력]: 5월 3일 홍길동 대리 출장 승인 요청 건
[출력]: { "date": "2026-05-03", "requester": "홍길동", "title": "출장 승인" }`
```

**Presenter Notes**:
> 백 마디 설명보다 하나의 확실한 예시가 낫다는 격언은 LLM에게도 그대로 적용됩니다. Few-shot               기법을 사용해 원하는 입출력 규격을 프롬프트에 끼워 넣어주면, 에이전트의 응답 안정성이 눈에 띄게 올라갑니다.

**Visual & Layout Guide**:
> 프롬프트 텍스트 내에서 'Example Area'가 분리되어 화살표로 최종 출력을 타겟팅하는               구조적 인포그래픽.

---

## Slide 7: 실습 안내: 회의록 데이터 구조화 실습
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
- **실습명:**비정형 회의록 텍스트의 멀티 포맷 데이터화 실습
- **실습 과제:**줄글로 작성된 미가공 회의록에서 결정사항과 태스크를 추출하여 아래 3가지 포맷으로 변환.
- 1. 인간 보고용**표 (Table)**
- 2. 신속 요약용**불릿 (Bullet)**
- 3. 시스템 연동용**JSON 스키마**
- **산출물:**3개 포맷 변환 결과물 및 장단점 분석 노트

**Presenter Notes**:
> 세 번째 실습을 시작합니다. 회의 진행 중 급하게 받아 적은 엉성한 회의록 텍스트를 제공해 드릴               것입니다. 이 비정형 원문에서 날짜, 할 일, 담당자를 뽑아내어 표와 불릿, 그리고 실제 API 연동이 가능한 JSON 규격으로 변환하는 강력한 프롬프트를 함께 작성해 보겠습니다.

**Visual & Layout Guide**:
> 실습 원문 텍스트 박스에서 3방향 화살표가 뻗어 나와 각각 표, 불릿, 코드 아이콘에 연결되는               인포그래픽.

---

## Slide 8: 실습 진행: 원본 회의록 및 스키마 정보
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
- **회의록 원문:**
  - `"5월 정기 회의에서 신규 교육 프로그램 홍보 일정이 논의되었다. 홍보 문안은 김민지가 이번 주 금요일까지 초안을 작성하기로 했다..."`
- **JSON 스키마 요구사항:**

**Presenter Notes**:
> 원본 회의록에는 여러 사람의 이름과 다른 요일의 기한들이 뒤섞여 있습니다. 이 정보를 정확하게 파싱하여               화면의 스키마 구조에 딱 들어맞는 JSON 데이터를 출력해 보십시오. 특히 날짜가 상대적인 '이번 주 금요일'인 경우 실제 달력을 기준으로 계산하도록 프롬프트를 고도화할 수도 있습니다.

**Visual & Layout Guide**:
> 좌측에 원본 회의록 원문을, 우측에 JSON 스키마 코드를 배치하고, 서로 연결되는 매핑 관계를               형광 펜으로 하이라이트 표기한 디자인.

---

## Slide 9: 공유 및 토론: 포맷별 비즈니스 활용처
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
- **토론 질문:**
  - 1. 사람이 읽고 액션을 취하기에 가장 가독성이 뛰어난 형식은 무엇이었나요?
  - 2. JSON 결과에서 불필요한 설명(예: "네, 준비된 결과입니다.")을 완전히 제거하기 위해 어떤 프롬프트 지시어를 추가했나요?
  - 3. 회의록에 명확한 기한이나 담당자가 누락된 경우, 에이전트가 오작동하지 않고 예외 처리를 수행하게 만든 노하우가 있다면?

**Presenter Notes**:
> 실습 결과를 바탕으로 이야기해 봅시다. JSON 출력에서 불필요한 서두를 제거하기 위해 어떤 제어               방식을 사용하셨나요? 또한 담당자가 빠진 업무는 어떻게 표기되도록 강제하셨는지, 서로의 솔루션을 비교해 봅시다.

**Visual & Layout Guide**:
> 3개 포맷의 활용 아이콘(보고서, 메신저, 데이터베이스)을 토론 질문과 결합하여 한눈에 들어오게               구성.

---

## Slide 10: 실무 팁: 안정적인 JSON 파싱 기법
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
JSON Schema 제약 제어 프롬프트복사
```python
고객 불만 피드백을 아래 JSON Schema 형태로 추출하세요. 
JSON 외의 어떤 설명 문자도 포함하지 마십시오.
{
  "type": "object",
  "properties": {
    "sentiment": { "type": "string", "enum": ["Positive", "Negative", "Neutral"] },
    "core_issue": { "type": "string" },
    "priority": { "type": "integer", "minimum": 1, "maximum": 5 }
  },
  "required": ["sentiment", "core_issue", "priority"]
}
```

**Visual & Layout Guide**:
> JSON 에러 예시 코드와 이를 예방하는 수정 코드를 나란히 띄워 차이점을 시각적으로 설명.

---

## Slide 11: 1일차 총정리 & 2일차 예고
- **Category**: 3차시. 출력 구조 제어 & 데이터화
- **Meta**: 1일차 | 에듀니티랩

### Content
- **1일차 핵심 개념 요약:**
  - **1차시:**에이전트는 입력을 해석하여 최종 판단 및 실행 결과를 만드는 비즈니스 파이프라인이다.
  - **2차시:**R-I-C-O 프레임워크를 적용하여 고유의 업무 기준과 전문성을 가진 Persona를 확보한다.
  - **3차시:**표, JSON 등의 출력 구조를 강제 제어하여 비정형 데이터를 유용한 정형 데이터로 변환한다.
  - **2일차 예고:**이제 단일 프롬프트를 벗어나**Workflow 설계, 조건 분기, 나만의 자동화 에이전트                       설계(Mini Project)**로 나아갑니다.

**Presenter Notes**:
> 이것으로 1일차 과정을 모두 마쳤습니다. 오늘 우리는 에이전트의 기본기인 개념 이해, 역할 부여,               그리고 출력 제어를 완벽히 정복했습니다. 내일부터는 여러 개의 프롬프트를 체인처럼 연결하는 Workflow 설계와 조건 분기 로직을 결합하여, 진짜 스스로 판단하고 단계적으로 일하는               에이전트를 조립해 보겠습니다. 수고 많으셨습니다. 내일 뵙겠습니다!

**Visual & Layout Guide**:
> 1일차 산출물들의 썸네일(질문 비교표, 페르소나 비교표, JSON 변환 결과)이 순서대로 정렬되는               시각적 피드백 제공. 우측에는 내일 다룰 3단계 Workflow 모형도 노출.                ---

---

